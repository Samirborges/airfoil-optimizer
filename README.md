# airfoil-optimizer

Intelligent airfoil optimization system using computer vision and aerodynamic modeling. Designed for high-performance RC aircraft by Corvos AeroDesign.

## 🚀 Como Executar o Projeto

Para facilitar a inicialização de todos os serviços (Backend e Frontend), este projeto conta com um script de automação para Windows.

### Pré-requisitos

Antes de começar, certifique-se de ter instalado:

- [Python 3.10+](www.python.org)
- [Node.js](nodejs.org)
- Ambiente virtual configurado na pasta `venv`

### Execução Rápida

Basta localizar o arquivo na raiz do projeto e executá-lo:

1. Dê um duplo clique no arquivo `.bat`.
2. O script abrirá duas janelas automaticamente:
   - **Backend:** Servidor Uvicorn rodando a API.
   - **Frontend:** Servidor de desenvolvimento do React (Vite).

_Nota: Certifique-se de que as dependências (`pip install` e `npm install`) foram instaladas previamente em suas respectivas pastas._
